1. Какие бывают варианты объявления переменных?
- с помощью let (var, но это более устаревший вариант)
let переменная;
перменная = значение переменной;
или
let перменная = значение переменной;
можно несколько переменных подряд через запятую объявлять в 1 строке- let перменная1 = значение переменной1, перменная2 = значение переменной2;

2. Как можно вывести сообщение "Hello World" с помощью JavaScript? (выберите один верный вариант)
-2. console.log('Hello World');

3. Что выведет этот скрипт?
- hello 1
hello name
hello  Ilya

4. Какое значение будет в z?
- 7

5. Какими тремя способами можно подключить JavaScript код?
-отдельным тегом в заголовке, в строке в конкретном теге или подключить внешний файл

6. Есть ли какая-то разница между записями `typeof str` и `typeof(str)`?
-`typeof(str)`используется чаще, результат будет выводиться и со скобками, и без них

7. Создайте переменную z, к ней присвойте переменные x+y и выведите результат в консоль. Допишите необходимый код вместо пробелов.
-const x = 5;
const y = 10; 
let z = x + y;
console.log(z);

8. Какой результат будет у выражений ниже?
-NaN

9. Что будет в x?
-  2

10. Что будет в x?
-  3

11. Как превратить переменную, содержащую строку, в число?
-x string = '5'; 
x stringAsNumber = Number(string); //какой код нужно подставить вместо ???, чтобы следующая строка вывела нам number?
console.log(typeof(stringAsNumber)); //number
